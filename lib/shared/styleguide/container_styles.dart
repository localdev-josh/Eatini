import 'package:flutter/material.dart';

final BoxDecoration selectedTabStyle = BoxDecoration(
    color: Color(0XFFF66C50).withOpacity(0.1),
    borderRadius: BorderRadius.circular(20.0)
);

final BoxDecoration defaultTabStyle = BoxDecoration(
    color: Colors.transparent,
    borderRadius: BorderRadius.circular(20.0)
);

final BoxDecoration
selectedFilterStyle = BoxDecoration(
  color: Color(0XFFFBD460),
  borderRadius: BorderRadius.circular(50.0),
);

final BoxDecoration defaultFilterStyle = BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.circular(50.0),
);